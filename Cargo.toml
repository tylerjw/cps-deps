[package]
name = "cps-build"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Common Package Specification (CPS) Rust Library"
repository = "https://github.com/tylerjw/cps-build"

[dependencies]
clap = { version = "4.5.3", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
